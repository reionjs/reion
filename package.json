{
  "name": "ReionJS",
  "private": true,
  "overrides": {
    "reion": "workspace:*"
  },
  "workspaces": [
    "apps/*",
    "examples/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --cwd examples/basic-api dev",
    "build": "bun run --cwd packages/reion build && bun run --cwd packages/reion-cron build && bun run --cwd packages/reion-better-auth build && bun run --cwd packages/reion-vitest build && bun run --cwd packages/devtools build",
    "test": "bun test",
    "lint": "echo \"lint: not configured yet\""
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "@types/node": "^24.0.0",
    "autocannon": "^8.0.0",
    "fastify": "^5.6.1",
    "typescript": "^5.9.0"
  }
}
